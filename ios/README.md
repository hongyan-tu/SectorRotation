// SwiftUI skeleton files are placeholders. Open Xcode and create a new SwiftUI app named SectorRotation.
// Add networking to call the backend /api/v1/dashboard endpoint and render charts with Swift Charts.

// Files to create in Xcode:
// - SectorRotationApp.swift
// - Views/DashboardView.swift
// - Models/Ranking.swift
// - Networking/APIClient.swift

// Example Networking pseudocode:
// struct APIClient {
//   func fetchDashboard(completion: @escaping (Dashboard)->Void) { ... }
// }
